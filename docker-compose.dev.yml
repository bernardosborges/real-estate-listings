networks:
  public-network:
  private-network:


services:

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile.nginx
    container_name: relist-nginx-dev
    depends_on:
      - backend
      - grafana
    networks:
      - public-network
      - private-network
    ports:
      - "8080:80"
      - "443:443"
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - certs:/etc/letsencrypt:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  certbot:
    build:
      context: ./certbot
      dockerfile: Dockerfile.certbot
    container_name: relist-certbot-dev
    env_file:
      - .env.local
    environment:
      GODADDY_API_KEY: ${GODADDY_API_KEY}
      GODADDY_SECRET_KEY: ${GODADDY_SECRET_KEY}
      CERTBOT_EMAIL: ${CERTBOT_EMAIL}
      DOMAINS: ${DOMAINS}
    networks:
      - private-network
    volumes:
      - certs:/etc/letsencrypt
    entrypoint: ["/bin/sh", "/certbot/certbot-entrypoint.sh"]

  # Local database for development
  db:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    container_name: relist-db-dev
    restart: always
    env_file:
      - .env.local # load environment variables from .env.local
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=pt_BR.UTF-8"
    networks:
      - private-network
    ports:
      - "5434:5432" # <host_port>:<container_port> (for acessing DB from host, e.g., pgAdmin)
    volumes:
      - ./db-init:/docker-entrypoint-initdb.d # Map project folder to docker entrypoint
      - ./db-templates:/db-templates # Map templates folder to docker
      - postgres_data:/var/lib/postgresql/data # persist DB data

  backend:
    build:
      context: . # Dockerfile is in the root of the project
      target: dev
    container_name: relist-backend-dev
    env_file:
      - .env.local
    depends_on:
      - db # Ensure DB starts before backend
    networks:
      - private-network
    ports:
      - "8000:8000"
    volumes:
      - .:/app # mount current folder inside container for development (live reload)
    command: >
      sh -c "
      alembic upgrade head &&
      uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      "
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  prometheus:
    image: prom/prometheus:latest
    container_name: relist-prometheus-dev
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/prometheus/alert.rules.yml:/etc/prometheus/alert.rules.yml:ro
    networks:
      - private-network
    ports:
      - "9090:9090"
    depends_on:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 5s
      retries: 3

  grafana:
    image: grafana/grafana:latest
    container_name: relist-grafana-dev
    networks:
      - private-network
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: relist-cadvisor-dev
    networks:
      - private-network
    ports:
      - "8081:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3

  node-exporter:
    image: prom/node-exporter:latest
    container_name: relist-node-exporter-dev
    networks:
      - private-network
    ports:
      - "9100:9100"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9100/metrics"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  postgres_data:
  grafana_data:
  certs:


# ==============================================================
# PostgreSQL Container Initialization Flow
#
# 1. Docker Compose starts the container for the first time.
# 2. The container mounts the data volume and maps the local
#    folder `./db-init` to `/docker-entrypoint-initdb.d/` inside the container.
# 3. The PostgreSQL entrypoint scans all files in `/docker-entrypoint-initdb.d/`.
# 4. It executes the files in **alphabetical order**:
#    - First, 01-setup-env.sh replaces environment variables provided to the
#    container (via env_file) into SQL templates.
#    - Then, 02-create-db.sql to create the database using pt_BR.utf8
#
# Notes:
# - This initialization **only happens on the first container startup**.
#   If the volume already exists, these scripts are ignored.
# - Files are executed in alphabetical order, so numbering them helps control
#   the initialization sequence.
# - Shell scripts need to be executable (chmod +x db-init/*sh) to run inside
#   the container.
#
#   docker exec -it relist-db-dev bash
#   chmod +x /docker-entrypoint-initdb.d/01-setup-db.sh
#   exit
# ==============================================================
