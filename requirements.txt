# Database
sqlalchemy>=2.0,<3.0
psycopg>=3.0,<4.0

# AWS S3
boto3

# Migration
alembic

# API and Schemas
fastapi>=0.128
pydantic>=2.0,<3.0
pydantic[email]
pydantic-settings>=2.0,<3.0

# Environment
python-dotenv==1.2.2

# Auth and security
bcrypt==4.0.1
passlib[bcrypt]
nanoid

# Image processing
pillow

# Image and file type validation
python-magic; sys_platform != "win32" # Linux version
python-magic-bin; sys_platform == "win32" # Windows-friendy

# Cache
redis

# Server
uvicorn[standard]

# JWT & HTTP clients
python-jose
requests>=2.31.0

# CLI / Logging helper
rich
# rqdoc; # Not necessary, because FastAPI already shows docs on swagger
